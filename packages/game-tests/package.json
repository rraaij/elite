{
  "name": "@elite/game-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@elite/game-core": "*"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  }
}
